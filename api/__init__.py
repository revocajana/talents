"""API connector package for the Talent management application.

This package exposes the API contract and serialization layer used to connect the
frontend with the Django backend. It is intentionally separate from the backend
Django project so the repository can keep the backend implementation in
`backend/` while still providing a top-level API interface.
"""
